.user-profile
  = render "user_info"
  = render "user_tabs"
  
  .info-bar The people #{@user.first_name} is following.
  
  %ul.avatars
    - @users.each do |user|
      %li
        = link_to(image_tag(user.avatar.url(:thumbnail)), user_path(user), :class => "tooltipped", :title => user.name)