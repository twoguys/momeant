- if @page && @page.has_styles?
  - content_for :head do
    :css
      #story_#{@page.story_id}_page_#{@page.number} {
        background-color: #{@page.background_color};
      }

.inner
  .title.bg-affected{:id => @page ? "story_#{@page.story_id}_page_#{@page.number}" : ""}
    .text= @page.text.html_safe if @page && @page.text
    %hr