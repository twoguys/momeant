.links
  = link_to("Back to your stories", stories_path)

.story-preview
  %h2
    Preview of
    %span.title= @story.title

  .excerpt= @story.excerpt
  
  %ul.topics
    %h3 Topics
    = @story.topics.map{|t| t.name}.join(', ')