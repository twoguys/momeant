.bookmarks
  %h1 Your Bookmarks
  
  %ul
    - @bookmarks.each do |bookmark|
      %li.bookmark
        %span.story= link_to(bookmark.story.title, bookmark.story)
        %span.date= time_ago_in_words(bookmark.created_at)