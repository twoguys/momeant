%li
  %span= editorial.user.name
  = link_to "edit", edit_admin_editorial_path(editorial)
  = link_to "delete", admin_editorial_path(editorial), :method => :delete, :confirm => "Are you sure?"
  .quote= editorial.quote